{
  "name": "ai-wingman",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "app": "npm start --workspace=app",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=app",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=app"
  }
}
